apiVersion: v1
kind: Service
metadata:
  name: coffeeshop-service
spec:
  selector:
    app: coffeeshop
  type: NodePort  
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 80
      nodePort: 31000
